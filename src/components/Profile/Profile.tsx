import { FC } from "react";
import cls from "../../style/layout.module.scss";

const Profile: FC = () => {
  return <section className={cls.layoutProfile}>profile</section>;
};

export default Profile;
